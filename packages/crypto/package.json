{
  "name": "@zerodrive/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Shared ZeroDrive encryption envelope helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zerodrive/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
